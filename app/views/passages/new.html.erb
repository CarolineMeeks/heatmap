<h1> New Passage </h1>

<%= simple_form_for :passage, url: passages_path do |p| %>
<% if @passage.errors.any? %>
  <div id="error_explanation">
    <h2><%= pluralize(@passage.errors.count, "error") %> prohibited
      this post from being saved:</h2>
    <ul>
    <% @passage.errors.full_messages.each do |msg| %>
      <li><%= msg %></li>
    <% end %>
    </ul>
  </div>
  <% end %>
  <p>
    <%= p.label :prompt %><br>
    <%= p.text_field :prompt %>
  </p>
 
  <p>
    <%= p.label :content %><br>
    <%= p.text_area :content, size: "100x12" %>
  </p>
 
<div>
    <%= p.submit "Save" %>
  </div>
<% end %>
