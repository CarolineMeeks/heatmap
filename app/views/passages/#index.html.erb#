<h1>Listing passages</h1>
 
    <th>Prompt</th>
    <th>Passage</th>
  </tr>
 
  <% @passages.each do |p| %>
    <tr>
      <td><%= link_to p.prompt, :action => "show", :id => p.id %></td>
      <td><%=h truncate(p.content, :length => 80) %></td>
    </tr>
  <% end %>
</table>
